<h1><%= @city.name %></h1>
<% @posts.each do |post| %>
  <p>
    <strong><%= post.title %></strong><br>
  </p>
<% end %>

<% if current_user %>
   <li><%= link_to "Add Post", new_post_path(current_user) %></li>
 <% end %>
</ul>
