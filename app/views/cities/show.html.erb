<h2><%= @city.name %></h2>
<% @posts.each do |post| %>
  <p>
    <strong><%= link_to post.title, posts_path(post) %></strong><br>
    <%= post.content %><br>
  </p>
<% end %>

<ul>
<% if current_user %>
   <li><%= link_to "Add Post", new_post_path(#) %></li>
 <% end %>
</ul>
